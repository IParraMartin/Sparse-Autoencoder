# Sparse Auto Encoder Implementaiton in PyTorch
This is an implementation of a Sparse Auto Encoder (SAE) in PyTorch. The loss is implemented from scratch, it uses MSE plus a penalty using KL divergence.

## Parameters
